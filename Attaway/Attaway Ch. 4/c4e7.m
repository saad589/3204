% Returns the calculated value of b 
c = input('Enter the value of c: ');
a = input('Enter the value of c: ');

if c > 0 && b > 0
    b = findb(c, a);
    fprintf('The value of b is %.1f\n.', b)
else
    fprintf('Error in input.')
end